import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.anishkanathayi.justgoofing',
  appName: 'Just Goofing',
  webDir: 'dist'
};

export default config;
